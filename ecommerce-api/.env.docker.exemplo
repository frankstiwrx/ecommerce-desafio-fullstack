# ecommerce-api/.env.docker
DATABASE_URL=postgres://postgres:postgres@db:5432/app?schema=public
JWT_SECRET=troque-isto
JWT_EXPIRES_IN=15m
CACHE_TTL=60

# E-mail (use o mesmo que já funciona pra você)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-password-or-app-password
MAIL_FROM="Minha Loja <your-email@gmail.com>"

# A API segue atendendo em localhost:3000 (mapeado pelo compose)
APP_URL=http://localhost:3000
